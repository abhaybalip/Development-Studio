spring.application.name=demo

# # H2 Database Configuration 
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.hibernate.ddl-auto=create-drop  # Important: create-drop for in-memory, use update for persistent
# spring.jpa.show-sql=true # Optional: to see the SQL queries in the console
# spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.H2Dialect #Explicitly set dialect (Optional but Recommended)

# DataSource configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.datasource.platform=h2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# H2 Console (optional)
spring.h2.console.enabled=true
